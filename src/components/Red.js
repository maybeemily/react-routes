import React from 'react';

export default function Red() {
  const style = {
    backgroundColor: 'red',
    width: '50vw',
    height: '50vw',
    fontSize: '4em',
    letterSpacing: '1em',
    color: 'black'
  };

  return (
    <div style={style}>RED</div>
  );
}
